import React, { useState } from 'react';
import { Box, Typography, Button, Paper, TextField, Grid, Container } from '@mui/material';
import { useNavigate } from 'react-router-dom';

const Home: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>홈</Typography>
    </Container>
  );
};

export default Home;

